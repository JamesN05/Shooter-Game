<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Shooter_Game.Settings"
             Title="Settings"
             BackgroundImageSource="menu_background.jpg">
    <VerticalStackLayout>
        <HorizontalStackLayout>
            <Button x:Name="Return_Button"
                    Text="Back Home"
                    Clicked="Return_Button_Clicked"
                    Background="SaddleBrown"
                    TextColor="LawnGreen"
                    FontAttributes="Bold"
                    FontFamily="PermanentMarker"/>
        </HorizontalStackLayout>
        <HorizontalStackLayout HorizontalOptions="Center">
            <Label Text="Settings"
                   FontSize="60"
                   TextColor="Red"
                   FontAttributes="Bold"
                   FontFamily="PermanentMarker"/>
        </HorizontalStackLayout>
        <HorizontalStackLayout HorizontalOptions="Center">
            <Label Text="Text Size"
                   FontSize="45"
                   TextColor="Red"
                   FontAttributes="Bold"
                   FontFamily="PermanentMarker"/>
        </HorizontalStackLayout>
        <HorizontalStackLayout HorizontalOptions="Center">
            <Button x:Name="Small_Button"
                    Text="Small"
                    Clicked="Small_Button_Clicked"
                    FontSize="20"
                    Margin="7"
                    Background="SaddleBrown"
                    TextColor="LawnGreen"
                    FontAttributes="Bold"
                    FontFamily="PermanentMarker"/>
            <Button x:Name="Medium_Button"
                    Text="Medium"
                    Clicked="Medium_Button_Clicked"
                    FontSize="20"
                    Margin="7"
                    Background="SaddleBrown"
                    TextColor="LawnGreen"
                    FontAttributes="Bold"
                    FontFamily="PermanentMarker"/>
            <Button x:Name="Large_Button"
                    Text="Large"
                    Clicked="Large_Button_Clicked"
                    FontSize="20"
                    Margin="7"
                    Background="SaddleBrown"
                    TextColor="LawnGreen"
                    FontAttributes="Bold"
                    FontFamily="PermanentMarker"/>
        </HorizontalStackLayout>
        <HorizontalStackLayout HorizontalOptions="Center">
            <Label Text="Auto-Save"
                   FontSize="45"
                   TextColor="Red"
                   FontAttributes="Bold"
                   FontFamily="PermanentMarker"/>
        </HorizontalStackLayout>
        <HorizontalStackLayout HorizontalOptions="Center">
            <Button x:Name="Local_Save_Button"
                    Text="Delete Local Data"
                    Clicked="Local_Save_Button_Clicked"
                    FontSize="30"
                    Margin="7"
                    Background="SaddleBrown"
                    TextColor="LawnGreen"
                    FontAttributes="Bold"
                    FontFamily="PermanentMarker"/>
        </HorizontalStackLayout>
        <HorizontalStackLayout HorizontalOptions="Center">
            <Label Text="Tips"
                   FontSize="45"
                   TextColor="Red"
                   FontAttributes="Bold"
                   FontFamily="PermanentMarker"/>
        </HorizontalStackLayout>
        <HorizontalStackLayout HorizontalOptions="Center">
            <Button x:Name="Tips_Button"
                    Text="On"
                    Clicked="Tips_Button_Clicked"
                    FontSize="30"
                    Margin="7"
                    Background="SaddleBrown"
                    TextColor="LawnGreen"
                    FontAttributes="Bold"
                    FontFamily="PermanentMarker"/>
        </HorizontalStackLayout>
    </VerticalStackLayout>
</ContentPage>